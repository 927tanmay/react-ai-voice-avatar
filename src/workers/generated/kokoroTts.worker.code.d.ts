/**
 * Build-time generated: the full Kokoro TTS worker source as a string literal.
 * Created by scripts/build-workers.mjs during `npm run build`.
 * This .d.ts stub is committed so `tsc --noEmit` passes in CI before build.
 */
export declare const kokoroWorkerCode: string;
